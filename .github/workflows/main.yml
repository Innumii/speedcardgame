
name: Build and Push Docker to GHCR

on:
  push:
    branches:
      - main
    workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write
      id-token: write

    strategy:
      matrix:
        service: [auth, cards, server]
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Push ${{ matrix.service }} Service
        run: |
          OWNER=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          docker build -f ${{ matrix.service }}/Dockerfile \
            -t ghcr.io/$OWNER/g4t2-${{ matrix.service }}-service:latest \
            ${{ matrix.service }}/
          docker push ghcr.io/$OWNER/g4t2-${{ matrix.service }}-service:latest
