version: "3.9"

services:
  game-server:
    build: .
    container_name: speedcardgame-server
    ports:
      - "4000:4000"
    restart: unless-stopped
    sysctls:
      - net.core.somaxconn=65535
    volumes:
      - ./certs:/certs:ro